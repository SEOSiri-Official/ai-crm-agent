name: SEOSIRI Daily Intelligence Ping
on:
  schedule:
    - cron: '0 9 * * *' # Every day at 9 AM
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Agent Sync
        run: |
          curl -X GET "https://aicrm.seosiri.com/api/agent"